images/
_source-meta.json

# Python bytecode cache from build.py
__pycache__/
*.pyc

# Local backup copies — never commit, never deploy
*.bak
*.bak-*
*.orig

# Local agent/editor state — already excluded from Vercel
.claude/
